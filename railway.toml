[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn webapp.app:create_app() --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
